# 字幕修订与片段重处理 — Tasks

## 总体顺序

按垂直切片推进。每个切片必须经过：规格检查 → 红测试 → 最小实现 → 全量测试 → 真实浏览器/真实子进程验证 → 独立提交。

## Slice A：UI 间距与配色打磨

- [ ] 测量固定播放栏桌面/移动端真实高度
- [ ] 用统一 CSS 变量消除 body、frame-main 和页面 section 的重复底部预留
- [ ] 验证有播放栏/无播放栏时最后一项内容不被遮挡且无过大空白
- [ ] 新增 5–6 套预设强调色，沿用深色/浅色主题
- [ ] 强调色统一驱动链接、激活态、进度条、字幕高亮和可交互 Tag
- [ ] 设置写入 localStorage，主 shell 与 iframe 同步
- [ ] 真实浏览器检查首页、详情、创作者、设置、任务中心、播放器和 700px 窄屏

## Slice B：单条字幕校正与安全快照

- [ ] 确认 `SubtitleRevisionStore` Interface
- [ ] 红测试：修改源文/译文后双 SRT 原子更新
- [ ] 红测试：任一文件写入失败时两份正式 SRT 都不变
- [ ] 红测试：首次修改建立自动生成基线；后续修改保存 previous 快照
- [ ] 红测试：负时间、倒序、超出媒体时长和重叠被拒绝
- [ ] 实现字幕文档读取、校验、原子提交、撤销和基线恢复
- [ ] ASGI 红测试：无会话/CSRF 的字幕写入被拒绝
- [ ] 播放器字幕行增加编辑 Dialog，保存后立即刷新当前字幕和列表
- [ ] 真实浏览器完成一次修改、撤销、恢复流程

## Slice C：字幕结构操作

- [ ] 红测试：合并连续字幕保留首尾时间并重编号
- [ ] 红测试：拆分点必须位于原时间范围内
- [ ] 红测试：源/译字幕无法确定对应关系时拒绝静默结构操作
- [ ] 实现合并、拆分和删除
- [ ] UI 增加连续行选择、合并、拆分和删除入口
- [ ] 真实浏览器验证播放中修改后高亮与跳转仍正确

## Slice D：Whisper 片段重处理闭环

- [ ] 确认 `SegmentProcessor.process()` Interface
- [ ] 红测试：连续字幕形成目标范围，不连续选择被拒绝
- [ ] 红测试：上下文范围扩展但正式替换范围不扩大
- [ ] 红测试：ffmpeg 截取失败不调用 ASR
- [ ] 实现短音频截取与临时文件清理
- [ ] 实现 `WhisperSegmentAdapter`
- [ ] 恢复相对时间偏移并裁切到目标范围
- [ ] 调用现有文本 LLM Profile 生成候选翻译
- [ ] ASGI 红测试：候选生成后正式 SRT 不变
- [ ] ASGI 红测试：只有确认候选才原子替换目标范围
- [ ] UI 增加处理配置、进度、取消和差异预览
- [ ] 用真实短音频跑通选择 → 候选 → 确认 → 撤销

## Slice E：Gemini Profile 与传输 Adapter

- [ ] 新增独立 GeminiAudioProfile 存储，密钥安全规则与 LLM Profile 对齐
- [ ] 配置 UI：名称、协议、Base URL、模型、默认模式、时长、prompt、代理、TLS、CA、Key
- [ ] 红测试：Google native 请求真实携带 inline audio、模型名和转写指令
- [ ] 红测试：OpenAI-compatible 请求真实携带 input_audio、模型名和转写指令
- [ ] 红测试：环境代理不劫持请求，Profile proxy 显式生效
- [ ] 红测试：Key 不进入 HTML、JSON、日志、SQLite、metadata 或事件
- [ ] 红测试：HTTP 200 空内容、语义空和格式错误有界重试
- [ ] 红测试：超过 60 秒配置限制/180 秒硬限制时调用前失败
- [ ] 实现两个 Transport Adapter 和共享重试/错误清洗
- [ ] Profile 测试走真实音频调用路径，不以 `/models` 代替

## Slice F：Gemini 片段候选

- [ ] 实现 `GeminiAudioAdapter`
- [ ] `transcribe_then_translate`：Gemini 源文 → 现有 LLM 翻译
- [ ] `bilingual_once`：严格解析源文和译文，缺任一项失败
- [ ] 忽略 Gemini 时间戳并按确定性规则复用目标范围
- [ ] 候选条数与原选择不一致时合并并显示警告
- [ ] UI 允许每次操作覆盖 Profile 默认处理模式
- [ ] 相同配置重试显示“结果可能相同”提示
- [ ] 用假 HTTP 上游完成全链路协议验证
- [ ] 有可用 Key/兼容网关时，用真实短片段分别验证两种协议；无真实凭据不得宣称集成已完成验证

## Slice G：归档浏览收尾

- [ ] 审计现有归档搜索、连续播放、失败恢复和字幕导出，避免重复实现
- [ ] 所有创作者标签可点击并按 creator_id 跳转作品浏览
- [ ] 搜索词、创作者筛选和分页互相保留
- [ ] 提供明确的清除筛选入口
- [ ] 真实浏览器验证作品详情、创作者页和统计页入口

## 发布 v0.6.0

- [ ] 全量 pytest 和 lint 全绿
- [ ] Node/JS 语法检查通过
- [ ] 真实浏览器完成 UI 与字幕修订闭环
- [ ] 真实 ffmpeg/Whisper 短片段闭环通过
- [ ] Gemini 两种协议通过假上游契约测试
- [ ] 只有存在真实凭据验证证据时，README 才声明对应 Gemini 协议“已实测”
- [ ] 审计 sdist/wheel，不包含媒体、字幕、日志、Key 或临时候选
- [ ] 更新 README、CHANGELOG、ROADMAP，打 v0.6.0 tag
