# SubForge Roadmap

> 定位与功能去留决策见 [ADR-0001](docs/adr/0001-product-positioning.md)，术语见 [CONTEXT.md](CONTEXT.md)。

## 定位

个人翻译工具：**同人音声（RJ 号）为主、niconico 直播为辅，日→中双语字幕**。开源公开（MIT），但以自用为先，不承诺社区服务。ASMR 预设是核心差异能力。

## 现状盘点（2026-08 基线）

### 功能去留

| 功能 | 决定 | 理由 |
|------|------|------|
| ASMR 预设 | **留（核心）** | 同人音声圈主力是 ASMR 耳语作品，是差异化护城河 |
| 断点续跑 | 留 | 长音频/批量刚需，已实现 |
| 模型缓存 | 留 | 本地/离线定位的根基 |
| 真实并行翻译 | 留 | 6000 条字幕 ~4 分钟的体验支撑 |
| 多文件并发 / 目录扫描 | 留 | 批量处理是真实场景 |
| 双语 SRT | 留 | 核心产出物 |
| Deepgram 云端 ASR | 留（质量选项） | 当初为质量引入，保留但文档注明费用/隐私 |
| 多目标语言 | **砍** | 受众只有日→中；实现只是改提示词，随时可加 |

### 已收口

- README、断点续跑、新字幕命名、打包元数据和 Library UI 文档已与实现同步。

---

## 路线图（1 → 2 → 3）

顺序依据：阶段 2 是壳、依赖阶段 1 把管线钉稳；阶段 3 技术风险最高（流式 + 低延迟 + 音频捕获），放最后。

### 阶段 1：批处理打磨 + 公开化

**目标**：管线稳定、可作为公开项目交付。

- 修正 README 与代码不一致（断点续跑状态、功能去留表、定位文案改 ASMR 核心）
- 补「开发中」缺失的收尾：`pyproject.toml` 元数据核对、release 流程（tag + CHANGELOG）
- 可选：GitHub Actions CI（pytest + uv sync）、issue 模板
- 验证 `uv tool install git+...` 路径可装可用

**验收**：README 与代码一致；`uv run pytest tests/ -q` 全绿；发布一个版本 tag（如 v0.3.0）。

### 阶段 2：本地作品库 UI（Slice 0–3 已完成）

**目标**：`localhost` 本地 Web UI（Python 起服务 + 浏览器打开），内存 < 200MB。

**形态**：封面墙 + 作品详情（音轨、字幕、中日对照）+ **内置播放器（字幕同步高亮）**。

- 作品识别：按文件夹/文件名中的 RJ 号识别作品
- 封面来源：自动去 DLsite 页面按 RJ 号抓取（需缓存与限频策略）
- 关联：音频文件路径 ↔ 源语言/翻译字幕 ↔ 时间轴

**开放问题**：作品库目录结构约定（现有文件夹怎么扫）；DLsite 抓取频率/失败降级（无封面显示占位）；播放器字幕同步方案。

**当前已完成**：单音频安全导入、RJ 作品/直播归档、可重建索引、命名 LLM Profiles、独立 Worker 队列、任务进度与操作、音频播放器和中日字幕同步。RJ 整包、DLsite 封面墙与视频抽音频分别留在 Slice 4、5。

**最终验收**：把自己现有的作品文件夹扫进库，浏览器内封面墙浏览、点开播放带中日对照字幕。

### 阶段 3：实时翻译

**目标**：niconico 直播等场景，读取系统音频，实时输出译文。

**形态（待定）**：WASAPI loopback 抓系统/浏览器输出音频 → 流式 ASR → 流式翻译（LM Studio 本地模型降低延迟）→ 实时字幕显示（悬浮窗/浏览器 overlay 未定）。

**可接受取舍**：延迟优先，丢几个词没关系。

**风险（最高）**：Windows 音频捕获方案；流式 ASR 选型（faster-whisper 无原生流式）；实时字幕显示形态。

**验收**：听直播时实时看到翻译，延迟可接受。

---

## 明确不做

- 多目标语言（改提示词即可，受众不存在）
- 公网作品展示站（商业 RJ 作品版权风险）
- GUI 断点管理界面
- 多用户 / 账号体系

## 推进方式

沿用现有 `.specs/<feature>/` 工作流：EARS 需求 → design → tasks，每阶段开工前先落规格再写代码。
